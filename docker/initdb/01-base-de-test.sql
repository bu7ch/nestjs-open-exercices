-- Exécuté par PostgreSQL au tout premier démarrage (volume vide) : la base des tests, à côté de
-- `marketplace`. Les tests en suppriment les tables : c'est pour ça qu'elle est à part.
CREATE DATABASE marketplace_test;
